import { type ComponentStyleConfig } from '@chakra-ui/react';

const Link: ComponentStyleConfig = {
    variants: {
        highlight: {
            color: 'primary',
        },
    },
};

export default Link;
